---
# This repository has no CI jobs of its own; it is consumed as a source
# archive by the stripe-internal-gocode-rocksdb prebuild jobs in stripe-internal/mint.
# The minimal job definition exists solely to satisfy the CI config parser.
henson_tarball:
  docker: {}
jobs:
  - name: stripe-private-oss-forks-rocksdb
    responsible_team: distributed-caching-platform
    henson_tarball:
      docker: {}
