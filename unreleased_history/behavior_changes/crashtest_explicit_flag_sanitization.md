`tools/db_crashtest.py` now sanitizes explicit passthrough `db_stress` flags before launching `db_stress`. If an explicit flag conflicts with the generated configuration, the script rewrites it to a compatible value and logs a warning; if two explicitly forced features conflict with each other, the script now fails fast instead of relying on gflags last-one-wins behavior or downstream C++ assertions.
