Added experimental `DB::ApplyLsmEdit()`, which replaces the contents of a key range with external SST files placed at levels the caller declares, atomically across column families. Unlike `IngestExternalFile()` it does not search for a level, so a multi-level set of files can be installed with its shape intact, and a range that holds no keys can be replaced without first compacting it to align with file boundaries.
